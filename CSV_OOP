# reading CSV file using OOP
import csv
from tabulate import tabulate

class CSV(object):

    def __init__(self):
        try:

         print tabulate(csv.reader(open("van.csv")))

         open("van.csv").close()

        except IOError:
            print "Something went wrong"

csv_obj = CSV()

